import React from "react";
import { StyleSheet, Text, View } from "react-native";

const friends = () => {
  return (
    <View>
      <Text>users' friend's lists</Text>
    </View>
  );
};

export default friends;

const styles = StyleSheet.create({});
